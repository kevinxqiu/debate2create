# Copy to .env for local experiments if desired. Do not commit real keys.
OPENAI_API_KEY=
GEMINI_API_KEY=
# GOOGLE_API_KEY is also accepted as a Gemini compatibility alias; set only one.
WANDB_API_KEY=
LLM_PROVIDER=
